{
  "name": "poc",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "author": "Brian Jenkins",
  "license": "MIT",
  "scripts": {
    "prebuild": "cd monaco && npm install && rm -rf dist",
    "build": "cd monaco && npm start #tsup monaco/src/main.ts --format esm --treeshake --outDir monaco/dist/",
    "interceptor": "node --experimental-specifier-resolution=node --loader=ts-node/esm interceptor",
    "tsup": "tsup monaco/src/main.ts --format esm --treeshake --outDir dist/"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "latest",
    "@types/node": "latest",
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "eslint": "latest",
    "ts-node": "latest",
    "tsup": "^7.2.0",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "sideEffects": false
}
